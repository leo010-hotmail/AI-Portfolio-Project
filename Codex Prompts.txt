This is a trading app that uses a natural language interface to interpret what actions users want to perform. The app prompts users for any missing information and once it has the information it needs to execute the order it executes it. Right now the app only has the ability to execute Buy and Sell orders using Market or Limit order types. Can you analyze the code and make the necessary changes to support order cancellation. The app should be able to interpret that the user wants to cancel an order, ask for any missing information needed to execute cancel order, provide a summary and request for confirmation before proceeding and once the order is executed provide a confirmation. If should also check if there is a matching open order to cancel, if not it should inform the user. 